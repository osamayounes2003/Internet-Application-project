import 'package:flutter/material.dart';
import '../../CustomComponent/BaseScreen.dart';

class Your_Screen_Name extends StatelessWidget {
  const Your_Screen_Name({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {

    ///put your controller here
  ///  final GroupsController groupsController = Get.put(GroupsController());

    return BaseScreen(
      child: Center(),
    );
  }
}
